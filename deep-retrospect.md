# 深度项目复盘（通用框架）

适用范围：**任何类型的项目**——代码开发、科学计算、数据处理、系统运维、ML 训练等。
本文件在 CLAUDE.md 触发关键词匹配时加载。

---

# 触发条件

以下任一满足即触发深度复盘：
- 同一项目/任务方向连续失败 ≥3 次
- 用户说"复盘""深度分析""来龙去脉""为什么总是失败""根本上有没有问题""反思"
- 换了 ≥2 种不同方向的修复后仍然失败（排除"参数微调能解决"的可能性）
- 同一错误跨环境/跨方案复现（排除环境特异性问题）
- 项目周期超过预期 3 倍以上仍未交付

---

# 核心原则

## CCRM（上下文污染重启模型）

**单纯重试失败的操作不会改善结果。** 每次失败后的上下文记忆会污染下一次尝试的判断——让你倾向于在已被证明无效的方向上微调，而不是质疑根本假设。复盘必须从零开始，完全抛弃之前失败尝试的思维惯性。

具体表现：你已经试了 5 次 VASP 提交都 OOM，每次都在降 ENCUT/改 NCORE 这些 Execution 层参数上打转。CCRM 要求你跳出来问：**POSCAR 里的真空层是不是从根上就设错了？**

## 根因 ≠ 表面症状

OOM 的修复（降 ENCUT）可能与真实根因（真空层过大导致 FFT 网格爆炸）完全无关。处理表面症状可能让问题暂时消失，但根因不除，换个体系又会复现。

## 盲区优先

多次失败中最稳定的不变参数恰恰最可能是根因。列出所有尝试中从未改变过的假设和参数——这些是盲区，优先审查。

---

# SAMULE 三层复盘框架

来源：Yubin Ge et al., "SAMULE: Self-Learning Agents Enhanced by Multi-level Reflection", EMNLP 2025

| 层级 | 范围 | 问题 |
|------|------|------|
| **Micro** | 单次失败 | 这一次具体哪里出错了？error_type 是什么？当时的参数快照？ |
| **Meso** | 同一项目的所有尝试 | 多次失败之间的共同模式？哪些参数始终没变（盲区）？跨尝试的共性？ |
| **Macro** | 跨项目/跨领域的通用教训 | 这个问题是否在其他项目/场景中也出现过？说明不是特异性问题，是方法论根因 |

**复盘的关键跃迁**：从 Micro（盯着每次的具体报错调参）跃迁到 Meso（看出共同模式）再跃迁到 Macro（认识到方法论层面的问题）。大多数失败的复盘停留在 Micro 层。

---

# AgentErrorTaxonomy 失败分类

来源：University of Illinois, "AgentErrorTaxonomy + AgentDebug", arXiv:2509.25370 (Sep 2025)

**所有项目失败都可以归入这三层之一。跨层误诊是最常见的复盘错误——在 Execution 层反复调参但根因在 Planning 层。**

| 层级 | 定义 | 通用示例 |
|------|------|---------|
| **Planning** | 开始执行前就存在的规划/设计错误 | 需求理解错误、架构设计有缺陷、输入数据本身有问题、选择了不适合的方法/工具 |
| **Execution** | 执行过程中的运行时失败 | 内存不足/OOM、超时、权限问题、依赖缺失、网络中断、API 限流 |
| **Response** | 执行成功但产出物有问题 | 结果不符合预期但不报错、输出格式错误、数据精度丢失、逻辑 bug 导致错误但能跑通 |

**典型盲区**：反复调整 Execution 层参数（内存/超时/重试次数）但根因在 Planning 层（选的框架根本不适合这个任务）。

---

# 通用复盘检查表

## A. 失败时间线（Micro 层）

每次失败的完整快照：
- 日期 + 尝试方案 + 关键参数/配置 + 具体的错误表现 + 为什么认为这次尝试失败了
- 如果有成功捕获（S6），标注与成功方案的差异

## B. 模式分析（Meso 层）

- [ ] 所有失败中有哪些共同特征？
- [ ] 有哪些参数/配置/假设在所有尝试中从未改变过？（**盲区清单**）
- [ ] 失败的频率是在增加还是在减少？（频率增加 = 方向可能完全错误）
- [ ] 是否所有失败都集中在同一层级（Planning/Execution/Response）？

## C. 根本假设审查（最关键的一步）

逐一列出项目开始时的所有前提假设，逐条验证：

- [ ] **工具/平台假设**：选择的工具/框架/语言是否适合这个任务？有没有更合适的替代？
- [ ] **数据/输入假设**：输入数据的质量、格式、完整性是否经过验证？
- [ ] **方法假设**：采用的方法/算法是否存在已知的限制或不适用场景？
- [ ] **环境假设**：运行环境（OS/依赖版本/硬件）是否是问题的来源？
- [ ] **需求假设**：对需求的初始理解是否正确？有没有被后续发现推翻？
- [ ] **时间/资源假设**：时间和资源估计是否从根上就偏低了？

## D. 联网搜索（强制）

```
# 搜索同类问题的已知解决方案
python websearch.py "<问题关键词> known issues troubleshooting 2024 2025"
python websearch.py "<工具/框架名> <错误类型> community solutions"

# 搜索替代方案
python websearch.py "alternative to <当前方案> for <任务描述>"
python websearch.py "<任务领域> best practices 2025"
```

搜索目标：这个问题在社区中有没有已知讨论？有没有人已经解决了？有没有更好的工具/方法我们一开始没考虑到？

## E. 跨项目参照（Macro 层）

- [ ] 我是否在之前的其他项目/领域见过类似的问题模式？
- [ ] 之前类似问题的解决方案是否可以迁移到当前项目？
- [ ] 这个项目的失败模式是否暴露了一个**我反复犯的系统性错误**？

---

# 复盘报告模板

```
═══════════════════════════════════════════
  深度复盘：<项目名称>
  时间跨度：<开始日期> → <当前日期>
  失败次数：N 次 | 成功次数：M 次
═══════════════════════════════════════════

▶ 失败时间线（Micro）
  1. [日期] [方案] → [错误类型] → [为什么判定为失败]
  2. [日期] [方案] → [错误类型] → [为什么判定为失败]
  ...

▶ 模式分析（Meso）
  共同主题：
  盲区（从未改变的参数/假设）：
  失败层级分布（Planning / Execution / Response）：

▶ 根本假设审查
  假设 1：[假设内容] → [验证方式] → [结论：成立/不成立/无法确定]
  假设 2：[假设内容] → [验证方式] → [结论：成立/不成立/无法确定]
  ...

▶ 联网搜索发现
  [搜索结果摘要 + 关键来源 URL]

▶ 跨项目参照（Macro）
  [之前是否见过类似模式？以前的解决方案？]

▶ 推荐新路径（按优先级排序）
  路径 A：[方案] — 与之前尝试的本质区别 — 置信度 [确信/大概率/待验证]
  路径 B：[方案] — 与之前尝试的本质区别 — 置信度 [确信/大概率/待验证]
  路径 C：[放弃当前方向，换方法] — 触发条件

▶ VERDICT
  [确信] 根因是 X → 执行路径 A，先用最小可行方案验证
  [待验证] 可能是 X 或 Y → 做对照实验排除
  [无法确定] 当前信息不足 → 建议先做哪些调查/实验
```

---

# 复盘后铁律

1. **报告给用户/决策者，禁止复盘后直接动手** —— 复盘结论可能涉及对需求或方向的根本性判断，必须确认后再执行
2. **一次只验证一个假设** —— 同时改多个变量 = 不知道哪个起了作用
3. **最小可行验证先行** —— 用最小的成本（时间/资源）先验证核心假设，确认有效再扩展
4. **记录每次验证结果** —— 成功和失败都写下来，持续丰富 Meso 层数据
5. **复盘报告本身就是产出** —— 即使项目最终失败，复盘报告是下一个类似项目最有价值的输入

---

# 与五大策略的联动

| 策略 | 在复盘中的角色 |
|------|---------------|
| S5 Cognitive Firewalls | Sunk-Cost Guardian 触发 → 停止盲目重试 → 进入复盘 |
| S6 Success Capture | 复盘找到方案 → 归档为永久规则 |
| **S7 Deep Retrospective** | 系统性分析失败 → 识别根因 → 建议新路径 |

---

# 何时结束复盘

复盘不是无限循环。以下任一满足即可结束：
- 找到了经小体系验证通过的新方案 → 进入正式执行
- 确认当前方法论无法解决 → 输出"不适用"结论 → 建议换工具/换方法/降低目标
- 用户明确表示不再继续该项目
- 已做了足够的小规模对照实验，证据指向明确结论
